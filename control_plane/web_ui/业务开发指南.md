# 🎯 AI政务业务系统 - 基于 vue-vben-admin 开发指南

> 使用 vue-vben-admin (Element Plus版本) 作为基础框架

## 📦 项目说明

本项目使用 **vue-vben-admin** 作为基础框架，这是一个功能完整的企业级后台管理系统，包含：

✅ 完整的布局系统（侧边栏、顶栏、多标签页）
✅ 权限管理（路由权限、按钮权限）
✅ 菜单管理（动态菜单、图标、国际化）
✅ 主题切换（亮色/暗色、主题色）
✅ 多标签页（页签管理、缓存）
✅ 请求封装（拦截器、错误处理）
✅ 表单封装（动态表单、验证）
✅ 表格封装（增删改查、分页）

---

## 🚀 快速开始

### 1. 启动开发服务器

```bash
# 在 admin-web 目录下
cd admin-web

# 启动开发服务器（默认启动 Element Plus 版本）
pnpm dev

# 或使用具体命令
pnpm dev:ele          # Element Plus 版本
pnpm dev:antd         # Ant Design 版本
pnpm dev:naive        # Naive UI 版本
pnpm dev:play         # Playground (更多功能演示)
```

### 2. 访问系统

- **地址**: http://localhost:5666
- **账号**: `vben`
- **密码**: `123456`

---

## 📁 项目结构

```
vue-vben-admin-main/
├── apps/
│   └── web-ele/              # Element Plus 版本 ⭐ 在这里开发
│       ├── src/
│       │   ├── api/          # API 接口
│       │   ├── views/        # 页面视图 ⭐ 添加业务页面
│       │   ├── router/       # 路由配置 ⭐ 添加业务路由
│       │   ├── locales/      # 国际化
│       │   └── ...
│       └── package.json
│
├── packages/                 # 共享包（框架功能）
│   ├── @core/               # 核心组件
│   ├── effects/             # 布局、表单、表格等
│   ├── locales/             # 国际化
│   └── ...
│
└── package.json
```

---

## 🎯 开发业务功能

### 第一步：添加业务页面

在 `apps/web-ele/src/views/` 目录下创建业务页面：

```bash
apps/web-ele/src/views/
└── business/                 # 业务管理
    ├── getihu/              # 个体户注册
    │   ├── list.vue         # 列表页
    │   ├── detail.vue       # 详情页
    │   └── form.vue         # 表单页
    ├── gongshang/           # 工商变更
    └── complaint/           # 投诉举报
```

**示例页面** (`views/business/getihu/list.vue`):

```vue
<template>
  <div>
    <Page title="个体户注册管理">
      <template #header-actions>
        <Button type="primary" @click="handleAdd">新增注册</Button>
      </template>

      <VxeBasicTable 
        :columns="columns" 
        :data="data"
        :loading="loading"
        @page-change="handlePageChange"
      />
    </Page>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { Page } from '@vben/common-ui'
import { Button } from 'element-plus'
import { VxeBasicTable } from '@vben/plugins'

const columns = [
  { field: 'id', title: 'ID', width: 80 },
  { field: 'businessName', title: '商户名称' },
  { field: 'status', title: '状态', width: 100 },
  { field: 'createTime', title: '创建时间', width: 180 },
]

const data = ref([])
const loading = ref(false)

// 获取数据
const fetchData = async () => {
  loading.value = true
  try {
    // 调用 API
    // data.value = await getGetihuList()
  } finally {
    loading.value = false
  }
}
</script>
```

---

### 第二步：添加路由配置

在 `apps/web-ele/src/router/routes/modules/` 目录下创建业务路由：

**创建文件**: `apps/web-ele/src/router/routes/modules/business.ts`

```typescript
import type { RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/business',
    name: 'Business',
    component: () => import('#/layouts/basic.vue'),
    meta: {
      title: '业务管理',
      icon: 'lucide:briefcase',
      order: 2,
    },
    children: [
      {
        path: 'getihu',
        name: 'BusinessGetihu',
        component: () => import('#/views/business/getihu/list.vue'),
        meta: {
          title: '个体户注册',
          icon: 'lucide:user',
        },
      },
      {
        path: 'gongshang',
        name: 'BusinessGongshang',
        component: () => import('#/views/business/gongshang/list.vue'),
        meta: {
          title: '工商变更',
          icon: 'lucide:building',
        },
      },
      {
        path: 'complaint',
        name: 'BusinessComplaint',
        component: () => import('#/views/business/complaint/list.vue'),
        meta: {
          title: '投诉举报',
          icon: 'lucide:alert-circle',
        },
      },
    ],
  },
]

export default routes
```

**注册路由**: 在 `apps/web-ele/src/router/routes/index.ts` 中导入

```typescript
import business from './modules/business'

export const routes = [
  // ...其他路由
  ...business,
]
```

---

### 第三步：添加 API 接口

在 `apps/web-ele/src/api/` 目录下创建接口文件：

**创建文件**: `apps/web-ele/src/api/business.ts`

```typescript
import { requestClient } from '#/api/request'

// 获取个体户列表
export async function getGetihuList(params?: any) {
  return requestClient.get('/business/getihu/list', { params })
}

// 获取个体户详情
export async function getGetihuDetail(id: string) {
  return requestClient.get(`/business/getihu/${id}`)
}

// 创建个体户注册
export async function createGetihu(data: any) {
  return requestClient.post('/business/getihu', data)
}

// 提交个体户注册
export async function submitGetihu(id: string) {
  return requestClient.post(`/business/getihu/${id}/submit`)
}
```

---

### 第四步：配置菜单（可选）

如果需要动态菜单，可以在后端返回菜单数据，或在前端配置：

**修改**: `apps/web-ele/src/api/core/menu.ts`

```typescript
export async function getAllMenusApi() {
  return [
    {
      id: 'business',
      name: '业务管理',
      path: '/business',
      icon: 'lucide:briefcase',
      children: [
        {
          id: 'business-getihu',
          name: '个体户注册',
          path: '/business/getihu',
          icon: 'lucide:user',
        },
        // ...其他菜单
      ],
    },
  ]
}
```

---

## 🔧 常用组件

### 1. Page 组件（页面容器）

```vue
<Page title="页面标题" description="页面描述">
  <template #header-actions>
    <Button>操作按钮</Button>
  </template>
  
  <!-- 页面内容 -->
</Page>
```

### 2. VxeBasicTable（表格组件）

```vue
<VxeBasicTable
  :columns="columns"
  :data="data"
  :loading="loading"
  :pagination="{ total: 100, pageSize: 10 }"
  @page-change="handlePageChange"
/>
```

### 3. VbenForm（表单组件）

```vue
<VbenForm
  :schema="formSchema"
  @submit="handleSubmit"
/>
```

### 4. VbenModal（模态框）

```vue
<VbenModal
  v-model:open="visible"
  title="标题"
  @ok="handleOk"
>
  <!-- 模态框内容 -->
</VbenModal>
```

---

## 📝 开发流程

### 典型的业务模块开发流程：

1. **创建页面文件** (`views/business/xxx/`)
   - `list.vue` - 列表页
   - `detail.vue` - 详情页
   - `form.vue` - 表单页

2. **添加路由配置** (`router/routes/modules/business.ts`)
   - 定义路由路径
   - 设置菜单信息（标题、图标）
   - 配置权限

3. **创建 API 接口** (`api/business.ts`)
   - 列表查询
   - 详情查询
   - 新增、编辑、删除

4. **开发页面逻辑**
   - 使用框架提供的组件
   - 调用 API 接口
   - 处理业务逻辑

5. **测试功能**
   - 功能测试
   - 权限测试
   - 样式调整

---

## 🎨 主题定制

### 修改主题色

编辑 `apps/web-ele/src/preferences.ts`:

```typescript
export const overridesPreferences = {
  theme: {
    colorPrimary: '#1890ff',
  },
}
```

---

## 🔗 相关文档

- [vue-vben-admin 官方文档](https://doc.vben.pro/)
- [Element Plus 文档](https://element-plus.org/zh-CN/)
- [Vue 3 文档](https://cn.vuejs.org/)
- [Vue Router 文档](https://router.vuejs.org/zh/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)

---

## 💡 提示

### 1. 使用框架提供的组件

框架已经封装了很多常用组件，优先使用：

- `Page` - 页面容器
- `VxeBasicTable` - 表格
- `VbenForm` - 表单
- `VbenModal` - 模态框
- `VbenButton` - 按钮

### 2. 保持代码风格一致

参考 `apps/web-ele/src/views/demos/` 下的示例代码。

### 3. 合理使用状态管理

简单的状态使用 `ref/reactive`，复杂的使用 Pinia。

### 4. API 统一管理

所有 API 接口都放在 `apps/web-ele/src/api/` 目录下。

---

## 🚀 开始开发

现在您可以：

1. ✅ 访问 http://localhost:5666
2. ✅ 使用 `vben / 123456` 登录
3. ✅ 查看示例页面（Dashboard、Demos等）
4. ✅ 在 `apps/web-ele/src/views/` 下创建业务页面
5. ✅ 在 `apps/web-ele/src/router/` 下添加路由

**祝您开发顺利！** 🎉

